// This file is used to import type definitions for React and React DOM.
// It doesn't contain any executable code, but it serves as a reference for the types used in the application.

// Import React and React DOM type definitions
import 'react';
import 'react-dom';

// You can add more type imports here if needed 